services:
  docs:
    image: wellsee_backend_local_docs
    container_name: wellsee_backend_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./wellsee_backend:/app/wellsee_backend:z
    ports:
      - '9000:9000'
    command: /start-docs
